# @package _global_

defaults:
  - _self_
  - data_settings@data: default   # folder=data_settings, mounts under cfg.data
  - model: null          # Keeps src/models agnostic
  - callbacks: default
  - logger: wandb
  - trainer: default
  - paths: default
  - experiment: null
  - debug: null

task_name: "train"
tags: ["dev"]
train: True
test: False

# Gold Standard Requirement:
deterministic: True
seed: 42
